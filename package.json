{
  "name": "seatlayer-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "SeatLayer SDK monorepo — the framework-agnostic core plus per-framework wrappers.",
  "type": "module",
  "scripts": {
    "sync:core": "node scripts/sync-core.mjs",
    "check:sync": "node scripts/sync-core.mjs --check",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist",
    "release:prep": "pnpm sync:core && pnpm check:sync && pnpm -r build && pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  },
  "packageManager": "pnpm@11.10.0"
}
